% git-tag(1) Version ${VERSION} | git-tag

# NAME

Shows or create tags

The listing shows their respective hash next to each other.

## FORCE WARNING

Note that if a tag is found and the `force` flag is set, the command
[git-tag-delete](git-tag-delete.md) is executed (By default on all submodules)

## SYNOPSIS

${SYNOPSIS}
